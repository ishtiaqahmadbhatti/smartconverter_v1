import '../../../app_modules/imports_module.dart';

class WebsiteScreenshotPage extends StatelessWidget {
  const WebsiteScreenshotPage({super.key});

  @override
  Widget build(BuildContext context) => const WebsiteToPngPage();
}
